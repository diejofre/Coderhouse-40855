export default class UserManagerMongo {
    constructor() {
        // instanciar el modelo
    }
    
    getUsers = () => {
        // return find de mongo 
    }
    
    saveUser = (user) => {
        // create(user) mongo
    }
}